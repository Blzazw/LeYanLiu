# 八皇后问题求解器

## 实现思路
使用回溯算法求解 N 皇后问题。算法逐行放置皇后，每次尝试在当前行的每一列放置皇后，并通过 `is_safe` 函数检查是否与已放置的皇后冲突（同一列或对角线）。当放置完所有行时，记录当前棋盘布局。最终返回所有解的列表，每个解是一个长度为 N 的列表，表示每行皇后所在的列索引。

## 运行方式
确保已安装 pytest：
```bash
pip install pytest